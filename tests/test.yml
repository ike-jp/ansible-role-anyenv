---
- hosts: localhost
  remote_user: root

  vars:
    anyenv_envs:
      - name: pyenv
        global_version: 2.7.9
        installation_versions:
          - 2.7.9
          - 3.5.1

      - name: rbenv
        global_version: 2.3.0
        installation_versions:
          - 2.3.0

      - name: luaenv
        global_version: sytem
        installation_versions: []
          #- 5.2.2

      - name: plenv
        global_version: system
        installation_versions: []
          #- 5.23.7

  roles:
    - yaegashi.blockinfile
    - ansible-role-anyenv

